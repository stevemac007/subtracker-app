## 1. Style Updates

- [x] 1.1 Increase `.clock-disp` font-size to 48px in `GlobalStyles.jsx`
- [x] 1.2 Increase `.cbtn` font-size to 18px and padding to 12px 18px in `GlobalStyles.jsx`
- [x] 1.3 Increase `.qbtn` font-size to 15px and padding to 12px 14px in `GlobalStyles.jsx`
- [x] 1.4 Scale Game Screen header `.hbtn` to 15px font and 12px 18px padding

## 2. Refinements

- [x] 2.1 Update vertical sort toggle in `GameScreen.jsx` to 14px font and 14px vertical padding
- [x] 2.2 Verify layout alignment of the clock bar with larger digits and buttons
- [x] 2.3 Adjust `.period-pill` alignment in `GlobalStyles.jsx` to stay vertically centered with larger buttons

## 3. Validation

- [x] 3.1 Verify all primary game controls meet the 44px-52px height standard
- [x] 3.2 Regression test: Ensure "ROSTER" and "HISTORY" on Home Screen still look balanced with new global `.hbtn` scaling
